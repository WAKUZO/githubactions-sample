name: Slack-notification

on: [push]

env:
  # SLACK_USERNAME: TestBot
  # SLACK_CHANNEL: develop
  # SLACK_WEBHOOK: https://hooks.slack.com/services/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
  SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
  
# jobs:
#   Explore-GitHub-Actions:
#     name: Slack Notification
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Hello World!"

    # steps:
    #   - name: Checkout
    #   # - uses: actions/checkout@v3
    #   - run: echo "Hello World!"
jobs:
  slackNotification:
    name: Slack Notification
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Success
      uses: rtCamp/action-slack-notify@v2.2.0
      if: success()
      env:
        SLACK_TITLE: Deploy flow / Success
        SLACK_COLOR: good
        SLACK_MESSAGE: Message for success

    - name: Failure
      uses: rtCamp/action-slack-notify@v2.2.0
      if: failure()
      env:
        SLACK_TITLE: Deploy flow / Failure
        SLACK_COLOR: danger
        SLACK_MESSAGE: Message for failure

      #-- Slack通知 --#
      # 成功
      # - name: Slack Notification on Success
      #   if: ${{ success() }}
      #   uses: rtCamp/action-slack-notify@v2
      #   env:
      #     SLACK_TITLE: Deploy Success
      #     SLACK_COLOR: good

      # # # 失敗
      # - name: Slack Notification on Failure
      #   if: ${{ failure() }}
      #   uses: rtCamp/action-slack-notify@v2
      #   env:
      #     SLACK_TITLE: Deploy Failure
      #     SLACK_COLOR: danger